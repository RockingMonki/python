# What is data ?

*Data* is anything that can be stored that is an input and an output.
Data in computers is stored in memory and there are types to it.

The types of data is stored differently so we use a concept called data types.

These are the primary data types of python:
- Int 
- Float 
- String 
- Bool
